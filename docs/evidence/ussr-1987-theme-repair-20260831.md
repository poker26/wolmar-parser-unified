# Приёмка ремонта тематических рублей СССР 1987 — 2026-08-31

## Что принято

Официальный matcher-коммит `59ca901` интегрирован в release-ветку как
`af5521c`. Ответ каталожной задачи интегрирован как `061d666`.

Проверено независимо:

- production-файлы `catalog/coin-matcher.js` и
  `catalog/dedupe-ussr-types.js` побайтово совпадают с `59ca901`;
- syntax-check обоих файлов проходит;
- `coin-matcher-denom`, `coin-matcher-real` и `slab-aware-valuation` проходят
  `66/66`;
- четыре контрольных production-заголовка возвращают:
  - Циолковский → `17639`, confidence `0.8`;
  - Октябрьская революция → `45796`, confidence `0.8`;
  - Бородино, барельеф → `45596`, confidence `0.9`;
  - заголовок без темы → тиражный `385479`, confidence `0.6`.

## Фактическое состояние production

Между записью handoff и приёмкой каталожная задача уже выполнила точечную
перепривязку:

- 84 лота Циолковского перенесены с `385479` на `17639`;
- 14 лотов Октябрьской революции перенесены на `45796`;
- 2 лота-набора и 2 неоднозначных/неподдержанных тематических лота отвязаны;
- на `385479` осталось ровно 60 лотов, и новый matcher оставляет все 60 на этом
  типе;
- повторный dry-run по `385479`: proposals `0`, hard conflicts `0`, unexpected
  destinations `0`.

Перенесённые строки имеют `match_method=repair-v3`; отдельные записи в
`lot_type_link_repair_log` для этого batch отсутствуют. У четырёх отвязанных
лотов остались старые snapshots `lot_type_link_quality`, но действующая оценка
соединяет quality по паре `(lot_id, type_id)`, поэтому эти строки в comparable
pool не попадают. Массовая очистка quality не запускалась.

## Эффект на единую оценку

После ремонта выполнен только read-only расчёт затронутых типов через текущий
production `ValuationService`; snapshots не записывались.

| Тип | Связей | Диапазон и медиана | Аналоги | Confidence |
|---|---:|---:|---:|---:|
| `385479`, тиражный 1 рубль 1987 | 60 | 1 133 / **1 333** / 1 678 RUB | 5 XF | 0.75 |
| `17639`, Циолковский | 116 | 80 / **80** / 86 RUB | 3 XF | 0.65 |
| `45796`, Октябрьская революция | 110 | 7 / **124** / 268 RUB | 24 ungraded | 0.76 |
| `45596`, Бородино, барельеф | 138 | 52 / **55** / 67 RUB | 6 XF | 0.80 |

До ремонта `385479` возвращал 80 / 86 / 1 333 RUB: медиану задавали свежие
лоты Циолковского, а верхнюю границу — тиражные рубли. После ремонта каждая
оценка формируется из своего предметного пула без изменения ценовой формулы.

## Доставка в API

Production-файл матчера был обновлён позже старта `wolmar-server`, а watch у
PM2 выключен. Поэтому перезапущен только `wolmar-server`; новый PID `437574`.
После рестарта:

- `/api/health/live` — `ok`;
- `/api/health/ready` — `ok`, database `up`;
- `temporal-parser-worker` не перезапускался и продолжает текущий Standart
  backfill.
